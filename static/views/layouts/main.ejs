<!DOCTYPE html>
<html ng-app="streamaApp" lang="en">
	<head>
		<meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
		<title><%= title %></title>
    <base href="<a href='<%= paths.root %>'></a>">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<link rel="shortcut icon" href="<%= assets.root + 'favicon.ico' %>" type="image/x-icon">
		<link rel="apple-touch-icon" href="<%= assets.root + 'apple-touch-icon.png' %>">
		<link rel="apple-touch-icon" sizes="114x114" href="<%= assets.root + 'apple-touch-icon-retina.png' %>">

    <link rel="stylesheet" href="<%= assets.css + 'style.css' %>">
    <script type="text/javascript" src="<%= assets.js + 'streama-app.js' %>"></script>

    <%# TODO: Include view-specific head %>
		<% if (view.head) { %>
      <%- include(view.head.path, view.head.data); %>
    <% } %>
	</head>
	<body>

		<header class="main" ng-if="!isCurrentState('player')">
			<a class="logo" ui-sref="dash">
        <img src="<%= assets.img + 'logo.png' %>">
      </a>

      <%# if logged in %>
      <% if (auth.user) { %>
        <ul class="navigation">
          <%# if user is content manager  %>
          <% if (auth.user.roles.indexOf("ROLE_CONTENT_MANAGER") != -1 ) { %>
            <li><a ui-sref="admin.shows">Admin</a></li>
          <% } %>

          <%# if user is admin but not content manager  %>
          <% if (auth.user.roles.indexOf("ROLE_ADMIN") != -1 ) { %>
            <% if (auth.user.roles.indexOf("ROLE_CONTENT_MANAGER") == -1 ) { %>
              <li><a ui-sref="admin.users">Admin</a></li>
            <% } %>
          <% } %>


            <li><a ui-sref="dash">Dashboard</a></li>
            <li><a ui-sref="profile">Profile</a></li>
            <li><a href="<%= paths.logout %>">Logout</a></li>

        </ul>
      <% } %>
		</header>

    <%# TODO: Include view-specific body %>
    <% if (view.body) { %>
    <%- include(view.body.path, view.body.data); %>
    <% } %>
	</body>
</html>
